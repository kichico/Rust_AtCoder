A: for i in (0..=n).rev()
B: 別の配列を用意して数字ならpush->その配列の先頭が0以外かつ長さがn-2ならOK
C: 何週するかを計算 && T%=(1週分) -> シミュ
D: O(NKD)で解けるはず、dp[i番目までみたとき][jこめまでつかったとき][総和がk mod dまでのとき]の3次元dp 遷移のときにはa[i]の値で決定すればよいけど頭がおかしくなっちゃった

